import 'package:flutter/material.dart';

class DrawerReportingPage extends StatelessWidget {
  const DrawerReportingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Reporting"),
    );
  }
}
